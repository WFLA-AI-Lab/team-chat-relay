# team-chat-relay 项目分析报告

> **角色视角**：资深项目管理顾问
> **分析范围**：项目立项（2026-08-16 前）至当前仓库状态
> **信息源**：仓库全部文件（当前状态）、`runtime/gpt-transcript.txt`（立项会话实录，255 行，session `01a00856-cfae-7e72-9647-0f2ce8a918c2`）、`SECURITY.md`、CI 配置
> **阅读对象**：项目决策者（社团负责人/管理员）与执行团队
> **约定**：每条关键结论附【依据】；无法从现有材料确认的事项一律标注【未知】，不以推测冒充事实。

---

## 1. 项目概览

### 1.1 名称 / 行业 / 业务领域

| 项 | 内容 |
|---|---|
| 仓库名 | `team-chat-relay` |
| 正式名称 | **Organization AI Agents**（Open WebUI 的 `WEBUI_NAME` 取值） |
| 行业 | 教育 / 学生组织数字化 |
| 业务领域 | 小型学生社团（约 10 人，高中 AI 社团）的**内部 AI 智能体服务平台**：聊天站 + Agent 创作擂台 + 教程站 |

【依据】`compose.yaml` 第 36 行 `WEBUI_NAME: Organization AI Agents`；`config/agents.json` 招新助手系统提示词「我们是一个学习人工智能的高高中生社团」；`tutorial/content/02-usage.md` 开篇「本社团的 AI 站点由三部分组成」。

### 1.2 缘起（立项背景）

项目源于负责人（用户 helen）的一次具体诉求：**让约 10 名成员共享其 GPT Plus 账号额度，预算 200 元以内**。

立项会话（Codex 桌面端 / GPT-5，2026-08-16 02:12→08:28 UTC）中的关键过程：

1. 助手明确拒绝协助 Cookie/会话共享（违反 OpenAI 服务条款、有封号风险）；
2. 用户多次坚持（「我是真的需要搭一个只用自己账号而不是 api」「我的账号无所谓啊」）；
3. 助手调研了多个第三方开源方案（chatgpt-web-share 已终止、zhaees/chatgpt-proxy、utksh1/conduit、wowyuarm/codex-proxy、OmniRoute），最终选型 **CLIProxyAPI（~35k stars，Go）+ Open WebUI** 组合，用 OAuth 登录而非共享 Cookie 的方式实现「自用账号中转」；
4. 选型过程中因预算与合规约束否掉了多个托管方案：AWS Lightsail（$24/月，用户「我每个月最多出50啊」）、腾讯云轻量（上游封锁+ICP 备案）、CloudCone 促销（缺货）；最终选定 **ServerCheap：2vCPU/4GB/40GB NVMe/5TB，美国达拉斯，$9/季度**（首付约 ¥61，摊到每月约 ¥20）；
5. 当晚完成服务器初始化（Ubuntu 24.04.3、2GB swap、relayadmin 用户、密钥登录）与全栈部署，并以 SSH 隧道在 `127.0.0.1:3000` 完成首次验证。

【依据】`runtime/gpt-transcript.txt`（81 条用户消息的完整对话流；服务器 IP/主机名/用户均出自该实录）。

### 1.3 当前阶段

项目已完成一次**重大架构转向**，现处于「**上线运行 + 持续迭代**」阶段：

- **转向**：从「CLIProxyAPI + Codex OAuth 账号中转」改为「**上游仅用 DeepSeek 官方 API**」。README 明确写道：「上游只使用 DeepSeek 官方 API，没有共享账号、没有 OAuth 代理，因此不存在『账号被封』的风险」。`login-codex.sh` 已从仓库移除，仅余空目录 `runtime/cliproxy-auth/` 作为历史残留。
- **功能扩展**（转向之后建设，无会话实录，时间线【未知】）：arena Agent 擂台子应用、tutorial 教程站（6 篇）、主站每日限额与模型锁定两个过滤器、按成员播种起始对话的脚本、备份/余额检查 cron、GitHub CI 校验。
- **当前形态**：三个对外服务 —— `chat.wfla-ailab.top`（主站，含 `/arena` 子路径）、`tutorial.wfla-ailab.top`（教程），Caddy 统一 HTTPS。

【依据】README「架构」与「安全说明」节；`arena/`、`tutorial/` 目录及模板；`functions/` 两个过滤器；`scripts/setup-cron.sh`。

**阶段判断**：属于「MVP 已上线、进入运营治理期」。技术骨架完整，但**治理层（版本控制、文档一致性、备份冗余、人员备份）尚未建立**——这是当前项目最突出的短板（详见 §5）。

---

## 2. 核心目标、预期成果与关键成功指标

### 2.1 核心目标

1. **合规且可持续的共享 AI 入口**：为约 10 名社团成员提供统一登录的 AI 聊天/Agent 服务，上游为 DeepSeek 官方 API，不依赖任何账号共享（彻底规避封号风险）。
2. **低成本**：月度总成本控制在 **¥50 以内**（服务器摊薄 ~¥20/月 + DeepSeek 按量用量）。
3. **低门槛创作**：成员无需碰服务器/Git，即可在网页上创建自己的 Agent，经管理员审核后上架。
4. **知识沉淀**：用教程站把「Agent 是什么、怎么创建、提示词怎么写、费用怎么省」沉淀为可自学的课程。

【依据】README 各节；`tutorial/content/00~05` 六篇教程（AI Agent 入门 → DSH 部署 → 本站使用 → 提示词技巧 → DeepSeek 与预算 → 流水线）。

### 2.2 预期交付物（现状：已基本齐备）

| 交付物 | 状态 |
|---|---|
| 主站：Open WebUI + PostgreSQL + Caddy，HTTPS | 已部署（生产可用性【未知】） |
| 4 个预置 Agent（通用助手/深度思考/代码助手/招新问答助手） | 已配置（`config/agents.json`），经 `sync-agents.py` 同步 |
| arena 擂台：提交→自动试跑→审核→上架、试聊、收藏、留言、12 周主题、新手引导 | 已建成（931 行自包含 Flask 应用） |
| 过滤器：主站每人每天 300 条、预置对话模型锁定 | 已装（`functions/` + `install-filter.py`） |
| 起始对话播种：每成员 × 每 Agent 一条带欢迎语的对话，并锁定对应模型 | 脚本就绪（`seed-starter-chats.py`） |
| 教程站：6 篇 Markdown 课程 | 已建成 |
| 运维脚本：bootstrap/deploy/backup/check-balance/setup-cron/validate | 已建成；cron：03:15 备份、08:30 余额检查 |

【依据】仓库文件清单与 `README.md` 第 1–9 节。

### 2.3 关键成功指标（KPI）

**文档中明确写下的（可直接采用）：**

| KPI | 目标值 | 出处 |
|---|---|---|
| 月度总成本 | ≤ ¥50 | 立项会话（用户自述上限）；`check-balance.sh` 余额 < ¥10 告警 |
| 主站消息限额 | 每人每天 300 条 | README §8.5 |
| arena 试聊限流 | 每人 10 条/分钟、100 条/天 | compose.yaml、README §8 |
| 数据备份 | 每日 03:15 pg_dump，保留 7 份 | `backup.sh`、`setup-cron.sh` |
| 余额巡检 | 每日 08:30 自动检查 | `setup-cron.sh` |
| 审核门禁 | 成员 Agent 默认私有，管理员显式公开 | README §6（**与 compose 实际值冲突，见 §5.2**） |

**推断的运营 KPI（建议与决策者确认是否采纳）：** 成员实际开通数（目标 ~10）、周活跃成员数、arena 每周提交/投票数、招新助手被咨询量、教程页面访问量。**这些目前均无任何采集手段，全部【未知】——项目没有任何使用量统计能力，这本身就是一个待办。**

---

## 3. 干系人分析

| 干系人 | 角色 / 职责 | 利益与关注点 | 影响力 |
|---|---|---|---|
| **负责人（helen）** | 唯一管理员 + 出资人 + 架构师 + 唯一部署/审核/运维人。管理员账号 `kangkang100402qw@hotmail.com`（显示名 ricykang402） | 预算安全（≤¥50/月）、不被封号、社团口碑、个人时间投入（高中生，时间稀缺） | 高（当前是单点） |
| **社团成员（约 10 人）** | 使用方；可创建 Agent、投票、收藏、留言；招新渠道的潜在受众 | 稳定可用、免费、响应快（立项原话「不做复杂推理时响应要快」）、自己的 Agent 能被看到 | 中（用户侧，无运维权限） |
| **所在组织（WFLA，疑为某机构 AI lab 缩写，全称【未知】）** | 场地/身份背书；域名 `wfla-ailab.top` 的持有方 | 社团活动成果展示、合规安全、对外形象 | 中 |
| **DeepSeek** | 唯一上游 API 供应商（按量付费） | 正常付费关系；条款合规使用 | 中（服务中断即全站停摆） |
| **ServerCheap** | VPS 托管（$9/季度，达拉斯节点） | 付费关系；**美国机房 → 中国大陆访问延迟是隐性体验问题** | 低 |
| **开源社区**（Open WebUI / Caddy / Flask） | 依赖方 | 版本锁定策略下仅被动跟随 | 低 |

**干系人结构的关键判断：这是典型的「单点负责人」项目。** 负责人一人同时承担出资、架构、部署、审核、客服、运维六重角色，且无第二管理员、无交接文档（除 README 外）。任何一项（时间、设备、账号、健康）缺位，项目即停摆。风险 §5.5 详述。

---

## 4. 现有资源、技术栈、数据源与已完成工作

### 4.1 基础设施

| 资源 | 规格 | 备注 |
|---|---|---|
| VPS（ServerCheap，relay01） | 2vCPU / 4GB / 40GB NVMe / 5TB 流量，美国达拉斯，$9/季度（首付约 ¥61，摊薄约 ¥20/月） | Ubuntu 24.04.3，2GB swap；IP `68.168.20.247`【敏感信息，正式文档建议脱敏】 |
| 服务器账户 | `relayadmin`（sudo + docker 组） | root 密码登录已禁用，仅密钥登录；仓库内含服务器私钥 `.ssh/relay_agent`（ed25519，已 gitignore） |
| 域名 | `wfla-ailab.top`（chat / tutorial 子域；arena 挂在 chat 域 `/arena` 子路径下，不另设子域） | Caddyfile 注释与教程正文提及；`.env.example` 保留 `example.org` 占位（真实值在服务器 `.env`） |

【依据】`runtime/gpt-transcript.txt`（购买与初始化过程）；`Caddyfile`；`.env.example`；`compose.yaml`。
【未知】域名 DNS 是否已实际解析、HTTPS 证书是否已签发、生产服务此刻是否在运行（本分析环境未配置 SSH，无法实测）。

### 4.2 技术栈

```
                    ┌─ chat.wfla-ailab.top ──┬─ Open WebUI v0.11.0 (8080, 仅回环)
Caddy 2.10.2 ───────┤                        ├─ arena Flask (python:3.12-slim, 8081, /arena 子路径)
(HTTPS/自动证书)     └─ tutorial.wfla-ailab.top ── tutorial Flask+Markdown (8082)

数据层：PostgreSQL 17.10-alpine（Open WebUI 用户/对话）
       + arena SQLite（arena_data 卷内 arena.db）
       + open_webui_data 卷（过滤器计数器 JSON）

上游：仅 DeepSeek 官方 API（deepseek-chat / deepseek-reasoner，OpenAI 兼容端点）
```

- 镜像全部**版本钉死**（`ghcr.io/open-webui/open-webui:v0.11.0`、`postgres:17.10-alpine`、`caddy:2.10.2-alpine`），SECURITY.md 明令生产不开自动更新。
- 安全基线：所有容器 `no-new-privileges`、Open WebUI 仅监听回环、关闭匿名遥测、关闭代码执行/联网搜索等可选面、`SAFE_MODE=true`、关闭注册（管理员发号）、会话 Cookie 强制 Secure。

【依据】`compose.yaml`、`SECURITY.md`、`.env.example`。

### 4.3 数据源

| 数据 | 位置 | 说明 |
|---|---|---|
| 用户、对话、Agent 模型定义 | PostgreSQL `openwebui` 库 | Open WebUI 主数据 |
| 过滤器计数（每日消息数、锁定映射） | `open_webui_data` 卷内 JSON 文件 | `daily_message_usage.json`、`locked_chat_models.json` |
| arena 全部数据（投稿/审核/投票/收藏/留言/试聊记录/流水线） | `arena_data` 卷内 SQLite | 自包含，与主站无共享 schema |
| Caddy 证书/配置 | `caddy_data` / `caddy_config` 卷 | 自动签发 |
| 备份 | 服务器本地（pg_dump，保留 7 份，03:15 执行） | **仅本地，无异地副本**（是否另有手工异地备份【未知】） |

### 4.4 已完成工作清单（仓库可证）

- 文档：`README.md`（217 行运维手册，9 大节 + 常用命令）、`SECURITY.md`；
- 11 个运维脚本（bootstrap / deploy / deploy-tunnel / sync-agents / install-filter / seed-starter-chats / backup / check-balance / setup-cron / validate，另含 2 个开发期探针残留）；
- arena 自包含应用：`app.py` 931 行 + 11 个模板（含 3 个流水线页面）；
- tutorial 站：Flask 渲染 6 篇课程；
- 2 个 Open WebUI 过滤器函数；
- CI：`.github/workflows/validate.yml`（PR/main 触发 `validate.sh`：compose 语法、`.env` 占位符、agents.json JSON 合法性）；
- 1 个本地干跑测试 `tests/test_seed_dryrun.py`（**当前不可运行，见 §5.4**）；
- 1 份立项会话实录存档（`runtime/gpt-transcript.txt`）。

---

## 5. 关键约束、潜在风险与未知信息

### 5.1 关键约束

1. **预算硬约束 ¥50/月**（用户原话上限；立项时说的「200 以内」是总额误读后的自我修正）。服务器摊薄 ~¥20/月，留给 DeepSeek 用量的余量只有 ~¥30/月 —— 对 10 人自由使用偏紧，所有一切限流设计都源于此。
2. **单维护者约束**：负责人为高中生，时间碎片化、技能仍在成长；所有变更都依赖「一个人 + 一个 AI 助手」完成。
3. **规模约束**：10 人小团队，不需要高可用架构，但需要「坏得起、修得快」的低运维负担设计。
4. **上游单点**：只有 DeepSeek 官方 API；其限流、涨价、服务中断无备选路径（README §7 自己也提示「5 人以上同时生成可能遇到限流」）。
5. **合规约束（正面）**：已彻底放弃共享账号路线，符合 OpenAI/DeepSeek 条款；但 arena 流水线中有一步「**降AI率**」（降低 AI 痕迹检测率），用于文章生成场景 —— 若成员用于作业/论文，存在**学术诚信风险**（详见 §5.6）。
6. **版本策略约束**：镜像钉死 + 禁止自动更新 ⇒ 升级是有意识的、手工的、带风险的窗口期（`seed-starter-chats.py` 直接写 PostgreSQL 内部表，对 schema 敏感）。

### 5.2 文档与实现不一致（本分析发现的最高优先级治理问题）

三处书面材料互相矛盾，任何新读者（包括未来的负责人自己）都会得到错误指引：

| # | 矛盾点 | 文档 A | 文档 B | 实际生效 |
|---|---|---|---|---|
| C1 | 模型访问门禁 | README §6：`BYPASS_MODEL_ACCESS_CONTROL=false`，「成员模型保持私有直到管理员手动公开（审核门禁）」 | `compose.yaml` 第 66 行：`=true`，注释称「审核门禁在 arena 侧」 | **`true`**（主站模型对全员可见） |
| C2 | arena 投票/排行榜 | README §8：「每周投票 + 周擂主、本周/总榜」作为核心玩法详述 | 教程 02-usage：「**擂台已停用投票/排行榜**，保留创建/收藏/留言/每周主题」 | 【未知】——arena 代码中投票逻辑是否已摘除需查 `app.py` 路由；README 与教程必有一处过期 |
| C3 | arena 流水线功能 | README 与教程 00–05 均未提及「流水线」（仅教程 05 提到一个共享演示流水线「文章生成流水线」） | `arena/templates/` 存在 3 个流水线页面（pipelines / pipeline_builder / pipeline_detail） | 功能存在但**文档体系未覆盖**，成员无从得知，管理员亦无操作指引 |

**影响**：C1 意味着 README 描述的「主站审核门禁」实际不存在（门禁实际在 arena 侧，README §8 的部署说明又反过来成立）——两套「推荐玩法」（§6 主站 Workspace 审核 vs §8 arena 审核）并存且互相打架，新成员/新管理员无法判断哪条流程才是官方流程。
**建议**：一次 30 分钟的文档对账（以 compose.yaml + 实际服务器行为为准）即可消除。

### 5.3 版本控制缺位（最严重的具体风险）

- `git log` 直接 fatal：**main 分支零提交**；工作区文件已 `git add` 进索引但从未 commit；**未配置任何 remote**。
- 而 README「常用命令」与 SECURITY.md 都建议「私有仓库 + 受保护分支」——**项目自己的文档要求它做它没做的事**。
- 后果：当前一切成果只存在于 (a) 这台 Windows 电脑的 Desktop 目录、(b) 服务器上的拷贝。任一载体损坏（硬盘故障、误删、勒索）即整体丢失；也无任何变更历史可回溯（本次发现的三处文档矛盾正是「没有 git 历史可查」的直接体现）。
- 附带问题：`.ssh/relay_agent` 服务器私钥位于仓库目录内（虽被 gitignore），一旦未来执行 `git add -A && commit` 疏漏，密钥将随代码流出。**初始化版本库时必须先确认私钥不出库，并考虑为服务器密钥单独建立访问策略。**

### 5.4 测试体系失效

- `tests/test_seed_dryrun.py` 执行的是 **不存在的文件** `scripts/_seed_test_copy.py`（仓库中无此文件）→ 直接报错；
- 其断言「应为 5 个 Agent 各建 1 条对话」，而 `agents.json` 实际只有 **4 个** Agent → 即便补上文件，断言也是过期的；
- CI（`validate.yml`）只跑 `validate.sh`（配置合法性），**不跑 pytest** → 这个坏测试既没被发现也没被拦截；
- arena（931 行核心自定义代码）与 tutorial **零测试**。
- 结论：项目当前**实际可用的回归保护只有「compose 配置合法」一条**。

### 5.5 单点故障与运维冗余

- **人员单点**：管理员=负责人=唯一持钥人。DeepSeek API Key、服务器私钥、admin 密码全部集中在一个人；无第二管理员、无应急恢复手册（SECURITY.md 有「泄露后轮换清单」，但没有「负责人失能时谁接手」的答案）。
- **备份单点**：pg_dump 只落在服务器本地卷，保留 7 份。服务器整机损坏（VPS 宿主机故障/机房事故）= 对话历史 + arena 全部数据 + Caddy 证书同时丢失。【未知：负责人是否另有手工异地备份习惯】
- **上游单点**：无备用模型路由；DeepSeek 限流/故障即全站不可用。
- **网络位置**：服务器在美国达拉斯，中国大陆成员访问延迟天然较高（立项时因国内节点封锁/ICP 问题被迫选海外——约束是真实的，但应在成员预期管理上说明）。

### 5.6 合规与伦理灰区（需决策者表态）

1. **「降AI率」流水线步骤**：arena 未文档化的流水线功能包含「审查兜底/降AI率」环节（模板可查）。若被成员用于让作业/论文绕过 AI 检测，项目可能被卷入学术诚信争议，且与社团「学习人工智能」的公开形象冲突。**建议：明确用途边界（如仅用于文案润色演示）、在页面加用途提示，或干脆下线该步骤。**
2. **招新助手的对外暴露**：`recruit-helper` 面向公众回答社团信息，其系统提示词允许「不确定的信息如实说明并建议联系管理员」——边界合理，但回答内容最终由模型生成，招新关键信息（时间/方式）建议改为可配置的事实清单而非自由生成。

### 5.7 遗留与卫生问题（低危但应清理）

- 空目录 `runtime/cliproxy-auth/`（CLIProxyAPI 时代残留）；
- `scripts/_probe5.sh`、`_probe6.sh`（开发期对 Open WebUI 源码的调试探针，无文档）；
- `.env.example` 与 Caddyfile 注释中的域名占位（`example.org`）与真实域（`wfla-ailab.top`）并存，信息分散在两处不同文件里；
- 立项实录 `gpt-transcript.txt` 内含真实邮箱、服务器 IP、购买细节——已在 `runtime/`（gitignore 覆盖）内，但**分发/展示该文件前需脱敏**。

### 5.8 未知信息清单（需一次 10 分钟的负责人访谈即可补齐）

| # | 未知项 | 影响 |
|---|---|---|
| U1 | 生产服务器当前状态：服务是否在跑、域名解析与 HTTPS 是否生效、何时正式上线 | 决定「当前阶段」判断的置信度 |
| U2 | 已实际开通并活跃的成员账号数（目标 ~10） | 判断项目是否真正「在使用中」 |
| U3 | DeepSeek 当前余额与月均实际花费 | 验证 ¥50/月 约束是否可持续 |
| U4 | 组织/社团的正式名称与 WFLA 的含义 | 报告抬头与对外口径 |
| U5 | 转向 DeepSeek 之后各功能（arena/tutorial/过滤器/播种）的实施时间线 | 重建项目时间轴 |
| U6 | arena 投票/排行榜到底是「停用」还是「未启用」（C2 矛盾裁决） | 文档对账 |
| U7 | 备份是否有异地副本；负责人失能时的接手人 | 单点风险评估 |
| U8 | 成员侧有无任何使用反馈/投诉渠道与记录 | 运营健康度 |

---

## 6. 项目全貌概述与优先澄清问题清单

### 6.1 一段话全貌

team-chat-relay 是一个**由高中生 AI 社团负责人在 2026 年 8 月 16 日一个约 8 小时的会话中从零启动、此后持续演进至今**的小型内部 AI 服务平台：一次因合规原因被否掉的「共享 GPT Plus 账号」诉求，被重构为「自有 VPS + Open WebUI + DeepSeek 官方 API」的合规架构（$9/季度服务器），并在其后长出了 Agent 创作擂台、教程课程、限额治理与运维脚本。它**技术上是一个完成度相当高的 MVP**——三个站点、四层权限、六篇课程、十一个脚本、一套 CI，全部由一人加一个 AI 助手完成；但**治理上仍是「一人项目」**：零 git 提交、三处文档互相矛盾、一个坏掉的测试无人拦截、备份与密钥全部系于一人。项目正处于从「建得出来」走向「长期跑得下去」的临界点，接下来一个季度最值得投入的不是新功能，而是**版本控制、文档对账与冗余建设**这三件不显眼但决定生死的事。

### 6.2 优先澄清问题清单（建议按序提问）

**P0 —— 影响项目存续（建议本周内回答）：**

1. **生产实况**：站点现在是否在线？域名与 HTTPS 是否已生效？已开通多少个成员账号，实际活跃几个？（对应 U1/U2）
2. **版本控制**：是否现在就执行「首次 commit + 建私有远程仓库」？执行前是否需要我协助做密钥脱敏检查（确认 `.ssh/`、`.env` 不入索引）？
3. **文档对账**：README §6（`BYPASS=false` 主站门禁）、README §8（arena 投票/排行榜为核心玩法）、教程 02（投票/排行榜已停用）三者冲突，以哪个为准？我可以直接按 compose.yaml 实际值订正另两份文档。

**P1 —— 影响可持续运营（建议本月内决定）：**

4. **预算实况**：DeepSeek 当前余额、近月花费多少？¥50/月 的余量还能撑多久？是否需要把每日 300 条限额收紧，或给成员加「按人配额」？（U3）
5. **单点冗余**：能否指定一名副手（哪怕只备份密钥和账号清单）？备份能否加一份异地副本（对象存储/网盘，成本 ≈ 0 元档）？（U7）
6. **「降AI率」流水线**：保留（加用途提示与使用边界）还是下线？这关系到社团的学术诚信形象。

**P2 —— 卫生与迭代（可排进下次迭代）：**

7. **仓库清理**：删除 `runtime/cliproxy-auth/`、`_probe5.sh`、`_probe6.sh`；修复或重写 `test_seed_dryrun.py`（补回被测文件、断言 5→4）；把流水线功能补进 README 与教程。
8. **运营度量**：是否接受「先不建统计，用每周五 10 分钟人工询问成员」的轻量 KPI 方式，还是值得花一次迭代加一个极简用量看板？
9. **下一步功能**：若治理项全部落地，下一个最有价值的功能是什么（新 Agent 主题？成员配额可视化？招新助手事实清单化？）

---

## 附录 A：证据索引（本报告引用的关键文件）

| 文件 | 用途 |
|---|---|
| `runtime/gpt-transcript.txt` | 立项会话实录（255 行；预算、选型、服务器、部署全过程） |
| `README.md` | 运维手册（架构、账号、Agent、arena、计费、备份、安全） |
| `compose.yaml` | 生产编排（实际生效的配置值） |
| `Caddyfile` | 域名路由与 arena 子路径挂载 |
| `.env.example` | 钉死版本与占位符约定 |
| `SECURITY.md` | 安全与泄露处置政策 |
| `config/agents.json` | 4 个预置 Agent 定义 |
| `arena/app.py` + `arena/templates/` | 擂台应用（含未文档化流水线页面） |
| `tutorial/content/00~05` | 6 篇课程（含「投票/排行榜已停用」声明、真实域名） |
| `scripts/*`（11 个） | 运维脚本全集 |
| `tests/test_seed_dryrun.py` | 现存唯一测试（当前不可运行） |
| `.github/workflows/validate.yml` | CI 校验范围 |
| `compose.tunnel.yaml` | 无域名过渡期编排 |

## 附录 B：敏感信息处理提示

本报告引用了真实服务器 IP、管理员邮箱等细节。**分发本报告前**：收件人限于项目决策者与执行团队；如对外引用，请替换 §4.1 的 IP 与 §3 的邮箱；`gpt-transcript.txt` 原文外发前需脱敏。
